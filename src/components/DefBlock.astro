---
const { h, p, href } = Astro.props;
---
<div class="def-block reveal"><h3>{h}</h3><p>{p}</p>{href && <a href={href} class="btn btn--ghost" style="font-size:.9rem;padding-top:.4rem">Ver detalle</a>}</div>
